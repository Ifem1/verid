# Demo fast path

After verified deployment: landing → connect injected wallet → /start profile → issue challenge → publish it on a genuine public surface → register HTTPS URL → /p/[id]/verify → show contract surface result and deterministic tier → issue proof → /proof/[id] → compatible /g/[id] → join on-chain → /g/[id]/room only after is_member=true.

Landing examples are illustrative, not live proof.

import fs from'node:fs';import path from'node:path';const d='dist';fs.rmSync(d,{recursive:true,force:true});fs.mkdirSync(path.join(d,'src'),{recursive:true});for(const f of['index.html','config.js','favicon.svg','vercel.json','live-surface.html'])fs.copyFileSync(f,path.join(d,f));for(const f of['app.js','protocol.js','styles.css'])fs.copyFileSync(path.join('src',f),path.join(d,'src',f));console.log('Built static Verid app to dist/.');
